import { Schema } from "@colyseus/schema";
export default class WinTileset extends Schema {
    FIRE: number;
    ICE: number;
    GROUND: number;
    NORMAL: number;
    GRASS: number;
    WATER: number;
}
